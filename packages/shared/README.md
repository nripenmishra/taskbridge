# Shared package (`@taskbridge/shared`)

Entry: `index.ts` — shared enums/constants for API and web.

When you add a build step, point `main`/`types` at compiled output or use TypeScript project references.
