# TaskBridge Web (Next.js)

Dev server: `npm run dev:web` from repo root (port 3000).

API base URL: set `NEXT_PUBLIC_API_BASE_URL` in `.env` (see `.env.example`).

See `docs/task-5-scaffold.md` for the full local setup.
